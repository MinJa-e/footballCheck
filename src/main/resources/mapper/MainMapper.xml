<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.footballCheck.mapper.main.MainMapper">
    <insert id="write" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO fcmain (category,title,content,thumbnail,writer,date)
        VALUES ( #{category}, #{title}, #{content}, #{thumbnail}, #{writer}, now() )
    </insert>

    <select id="getList" parameterType="com.footballCheck.pagination.Criteria" resultType="com.footballCheck.domain.main.MainDTO">
        <![CDATA[
        SELECT
            no,
            category,
            title,
            content,
            thumbnail,
            writer,
            date,
            view,
            good
        FROM
            fcmain
        ORDER BY
            no DESC
        LIMIT
            #{pageStart}, #{perPageNum}
    ]]>
    </select>

    <select id="countList" resultType="Integer">
        <![CDATA[
        SELECT
            count(*)
        FROM
            fcmain
    ]]>
    </select>
</mapper>